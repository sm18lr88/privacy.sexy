inputs:
  working-directory:
    required: false
    default: '.'
runs:
  using: composite
  steps:
    -
      name: Install the committed dependency tree
      shell: bash
      run: npm ci --engine-strict --strict-peer-deps
      working-directory: ${{ inputs.working-directory }}
